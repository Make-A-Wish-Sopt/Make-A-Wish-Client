import { QueryKeyType } from '@/types/queryKeyType';

export const QUERY_KEY: QueryKeyType = {
  itemData: 'itemData',
  wishesData: 'wishesData',
  payReady: 'payReady',
  pgToken: 'pgToken',
  PROGRESS: 'progress',
};
